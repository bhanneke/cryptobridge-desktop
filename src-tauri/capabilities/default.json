{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. core:default covers the webview basics and the event listener the BisqAdapter's transport needs for the 'bisq-ws' frame stream. The bisq_* commands are defined by this app rather than a plugin, so they are reachable without a permission entry — their allowlisting lives in src-tauri/src/proxy.rs, not here. Extend deliberately.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
