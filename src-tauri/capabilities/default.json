{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. The UI needs no IPC yet; core:default covers the webview basics. Extend deliberately when the BisqAdapter lands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
